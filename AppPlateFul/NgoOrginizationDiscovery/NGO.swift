import Foundation

struct NGO {
    let id: String
    let name: String
    let desc: String              // short bio (card)
    let fullDescription: String   // long bio (details)
    let area: String
    let rating: Double
    let reviews: Int
    let logoURL: String
    let phone: String
    let email: String
    let address: String
    let verified: Bool
}

import UIKit

// This struct is shared between History & Details
// Matches what you are passing right now (NO date, NO status)
struct DonationUpdate {
    var image: UIImage?
    var name: String
    var orderSummary: String
}
